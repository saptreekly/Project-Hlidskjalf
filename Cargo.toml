[workspace]
members = [".", "fuzz"]

[package]
name = "hlidskjalf"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"

[dependencies]

[features]
fuzzing = []
